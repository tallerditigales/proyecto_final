-Consulta sobre el codigo en ARM y las memorias de datos:

	¿El código que hicimos (creemos que) lee y escribe sobre los datos que están en una misma memoria, la memoria del simulador visUAL, pero escuchamos que en la clase dijo que eran 2 memorias además de la de instrucciones, una que tiene las frases que carga el usuario y otra donde se escribe la salida por asi decirlo.
	Estamos pensandolo mal?
	Tenemos la idea de que si son diferentes entonces meter un ciclo previo a lo que tenemos y escribir la frase completa en la memoria de escritura y luego aplicar el algoritmo, pero que nos recomieda


-Consulta sobre el modulo de memoria ROM para instrucciones:

	En clase usted dijo que la salida q de la memoria de instrucciones deberia ser de 32 bits, entonces me surge la duda de que si entonces la ROM que yo creo en quartus deberia tener palabras de 32 bits en bits de 8 bits, en ese caso, si las palabras son de 32 bits entonces podriamos utilizar el contador que va de 1 en 1 y no de 4 en 4?
	O es estrictamente necesario que el tamaño de las palabras de la memoria ROM sea de 8 bits y hay que hacer la logica en algun modulo para crear la instruccion concatenando las 4 partes de la instrucción.